import React from 'react'
import { View, Text, SafeAreaView } from 'react-native'
import ImagenScreen from './ImageScreen'

const Camera = () => {
    return (
        <SafeAreaView>
            <ImagenScreen/>
        </SafeAreaView>
    )
}

export default Camera